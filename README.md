# Benchmark Machine Test

### PHP project
- Take clone and add studentApi in your localhost path
- Create `studentApi` DB
- Import `student.sql` file into `studentApi` DB

### React JS 
- `npm install`
- `npm start`